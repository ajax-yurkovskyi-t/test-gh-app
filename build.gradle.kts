plugins {
    id("delta-coverage-conventions")
    id("kotlin-conventions")
}

allprojects {
    group = "сom.example"
    version = "0.0.1-SNAPSHOT"
    repositories {
        mavenCentral()
    }
}
